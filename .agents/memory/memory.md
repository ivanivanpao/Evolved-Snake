# AI 記憶檔案 (memory.md)

## 專案基本資訊
- **專案名稱**：5_15
- **專案路徑**：`c:\Users\ivani\OneDrive\桌面\web\web-frontend-dev\5_15`
- **技術棧**：Vue 3 + Vite + TypeScript + Pinia + Vue Router
- **最後更新**：2026-05-28

## 專案概況
- 這是一個以 Vue 3 + Vite 為基礎的**貪食蛇遊戲（Snake Game）**專案
- **經典模式**核心元件：`src/components/SnakeGame.vue`
- **Boss 戰模式**獨立元件：`src/components/BossGame.vue`
- `App.vue` 透過 `gameMode` 狀態條件渲染兩個模式，支援雙向切換

## 待處理事項
- [x] 清除預設範本殘留檔案（於 2026-05-21 順利清理完畢）
  - 已刪除：`HelloWorld.vue`、`TheWelcome.vue`、`WelcomeItem.vue`、`icons/`、`HomeView.vue`、`AboutView.vue`、`stores/`、`router/`、`__tests__/`
  - 已清理：`main.ts` 內引用的 `pinia` 與 `vue-router`，精簡專案載入架構
- [x] 新增進入遊戲與繼續遊戲時的 `start.mp3` 音效播放（於 2026-05-27 順利實作完畢）
  - 已在點擊「戴上墨鏡 😎 進入遊戲」按鈕、遊戲重新開始、按方向鍵開始遊戲 (`startGame`) 時播放 `/start.mp3`。
  - 已在繼續遊戲 (`resumeGame` / 空白鍵繼續) 時播放 `/start.mp3`。
  - **架構升級 (2026-05-27 修正)**：為解決非同步 Promise.then 導致多重 HTMLAudioElement 被瀏覽器判定為無手勢 Autoplay 阻擋的 Bug，徹底將音訊架構重構為「全域唯一 HTMLAudioElement (`sunAudio`) 實例共享與 src 動態切換模式」。
  - **同步手勢播放**：當玩家點擊首頁「戴上墨鏡」按鈕時，在點擊事件 the 同步一線 Callstack 中直接播放 `/start.mp3`，100% 攜帶使用者互動憑證繞過瀏覽器阻擋，同時完成實例與 Context 的全面解鎖！
- [x] 新增進入遊戲後循環播放 `back.mp3` 背景音樂（於 2026-05-27 順利實作完畢，並於同日修復啟動無聲 Bug）
  - 已建立全域獨立背景音樂 `backAudio` 實例，設定 `/back.mp3` 循環播放（`loop = true`）並以舒適音量（`0.25`）作背景渲染。
  - **Autoplay 最佳化解鎖**：在玩家點選「戴上墨鏡 😎 進入遊戲」的同步點擊事件 Callstack 中，立即觸發 `backAudio.play()`，100% 繞過瀏覽器的無手勢自動播放阻擋限制。
  - **啟動無聲 Bug 修復**：修復了 `closeWarningOverlay` 函數中誤使用未定義變數 `soundMuted.value` 的 Bug，將其修正為實際存在的 `bgmEnabled.value`。這解決了「背景音樂明明是 ON 卻沒有聲音，必須先關掉再打開才有聲音」的問題，讓玩家進入遊戲時能直接享受到震撼的背景音樂。
  - **資源釋放**：在元件 `onUnmounted` 時執行清理，徹底暫停播放並釋放 `backAudio` 實例以防止任何記憶體洩漏。
  - **貼心音量警示**：在剛進入遊戲的全黑遮罩儀式感頁面上，新增了一行具備呼吸漸變微光發光效果的提示字句 **「🔊 貼心提醒：進入後將播放背景音樂，請注意音量大小」**，提升使用者體驗。
- [x] 重構音訊控制為獨立的 BGM、SFX 與 Start 啟動音效系統，並在設定中提供音量滑桿與開關（於 2026-05-27 順利實作完畢）
  - **獨立狀態控制**：宣告 `bgmEnabled`/`bgmVolume`、`sfxEnabled`/`sfxVolume` 以及 `startEnabled`/`startVolume` 響應式狀態。
  - **精緻設定 UI**：在「⚙️ 設定」中以高質感的青（BGM）、綠（SFX）、紫（Start 啟動）三色流光滑桿與毛玻璃面板精緻呈現，供玩家針對各類音訊進行獨立開關與音量精確調整。
  - **太陽慶祝音效精簡**：遵循使用者要求，廢棄 `China_2.mp3` 音訊，將百分慶祝太陽特效音效**統一調整為僅播放 `China.mp3`**。
  - **Web Audio API 合成音量同步**：吃食物「叮」電子音效、太陽蛻皮、緩速、護盾等合成音效之基礎 Gain 增益係數完美與 SFX 音量滑桿變動（`sfxVolume`）進行等比例即時縮放，達成全遊戲音訊設定的高度大一統。
- [x] 實作 Boss 戰模式（於 2026-05-28 實作完成）
  - 建立 `BossGame.vue` 獨立元件（~1500 行），包含 Boss 4x4 太陽區域、日冠砲彈系統、GCD 數學大招問答、棉花補血、勝利/失敗判定、Boss 速度排行榜
  - 修改 `App.vue` 為雙模式路由入口（gameMode 條件渲染）
  - 在 `SnakeGame.vue` toolbar 新增 ⚔️ Boss 戰模式切換按鈕 + defineEmits
  - **細節調整與除錯**：
    1. **警告遮罩無縫修復**：修正了從 Boss 戰切換回經典模式時，`SnakeGame.vue` 狀態重置導致預設開啟「墨鏡警告遮罩」的 Bug，改為檢查 `localStorage` 若已有 ID 即自動跳過遮罩並恢復背景音樂。
    2. **日冠砲彈降速優化**：因原先砲彈與蛇綁定相同迴圈速度（200ms）過快，已將砲彈移動邏輯獨立出 `bossProjectileMoveInterval` 計時器，並調降速度至 400ms 移動一格，給予玩家合理的閃避空間。
    3. **OMG 大招音效實作**：在設定面板新增獨立的「大招音效 (OMG)」開關與音量拉桿，並實作於每次送出 GCD 問答答案時，觸發播放 `OMG.mp3`。
    4. **右側面板 UI 溢出修復**：修正了 `BossGame.vue` 右側資訊面板（`.side-panel`）因內容過多而在低解析度螢幕被切掉的問題，已加上 `max-height: 85vh` 限制與專屬客製化科技風格的垂直捲軸 (`overflow-y: auto`)。
    5. **儀式感進入遮罩修復**：修正了重新載入網頁時因 `localStorage` 殘留 ID 而錯誤跳過「戴上墨鏡進入遊戲」畫面的問題，透過 `App.vue` 引入全域 `hasEnteredGame` 狀態，確保只有在切換模式時才無縫跳過。
    6. **排行榜髒資料過濾**：修正 Boss 速度榜出現 `00:00 abc` 異常紀錄的問題。現在 `onMounted` 讀取與勝利寫入時，都會嚴格過濾 `time <= 0` 的無效成績，並自動清掉 `localStorage` 內的髒資料。
- [x] 重構 Boss 戰之太陽神殿技能商店系統（於 2026-05-28 實作完成）
  - **太陽神速 (Shift)**：消耗 1 ☀️，立刻將蛇的移動速度加倍（間隔時間除以二，最低限制 50ms），大幅增強競速快感。
  - **烈日緩速 (Q)**：消耗 2 ☀️，增加日冕砲彈的移動間隔時間 50ms（上限限制 600ms），顯著降低砲彈速度，提升玩家在彈幕中的閃避操作性。
  - **日冕護盾 (E)**：消耗 3 ☀️，獲得 5 秒無敵狀態。在此期間蛇身會呈現璀璨高亮的白金金色流動發光特效，且允許蛇頭安全穿過自己身體，不引發 Game Over。
  - **說明書與 UI 對齊**：同步重寫了 `doc/rules.md` 中的 §2.5 技能條款，並更新了 `BossGame.vue` 神殿商店、操作說明與側邊欄面板中的文案描述，保持高度一致性。

## 版控狀態
- `git init` 已執行（2026-05-21）
- 已提交 `chore: 清除預設範本殘留檔案並精簡 main.ts 架構`（2026-05-27）
- 已提交 `docs: 新增貪食蛇遊戲規則說明書`（2026-05-27）

## 對話記錄摘要
- 使用者希望先完成遊戲功能後，再回頭清理範本殘留檔案
- **2026-05-21 更新**：已成功實作 150 分（及其倍數）的日食「黑太陽殘影」特效。黑太陽圖片 `sun_black.png` 會以半透明、微幅偏移、專屬相位差抖動旋轉的 CSS 動畫，微微重疊出現在主太陽之上，完美展現使用者的驚喜設計！同時，為了讓玩家有充足時間觀賞這項驚艷設計，已將所有太陽全螢幕慶祝特效（包含普通太陽、重疊太陽、純黑太陽、分數與文字）的顯示持續時間與對應 CSS 動畫時長同步加長至 **3.5 秒**（3500ms），使畫面更具電影般緩慢震撼的張力！
- **2026-05-21 進階更新（完整關卡驚喜特效劇本）**：
  - **150分前**（每 50 分觸發，即 50、100分）：觸發「普通太陽」特效，顯示「太陽越來越耀眼」。
  - **150 分**：觸發「重疊太陽」特效（普通太陽 + 黑太陽殘影），顯示「好像出現了什麼?」。
  - **200 分**：觸發「重疊太陽」特效，顯示「越來越明顯」。
  - **250 分**：觸發「重疊太陽」特效，顯示「快看清楚了」。
  - **300 分**：觸發「純黑太陽」特效（只顯示黑太陽，帶有白日冕光暈），顯示「你被曬黑了」。
  - **細節調整**：已依使用者要求，從 HTML、CSS 與 Script 中**徹底移除了不必要的標題元素 (`sun-label`)**，保持畫面乾淨整潔。
  - **2026-05-21 加速功能開關**：已實作「加速功能開關」按鈕（位於棋盤上方工具列，啟用時呈現科技感橙黃色發光，停用時則顯示為防護盾防禦風 🛡️）。當關閉加速時，遊戲會貼心地自動將蛇當前的移動速度重設回基礎速度 `baseSpeed` (200ms)；當開啟時，在對應的分數太陽特效點才會執行速度提升（每次加減 15ms），讓玩家能自由掌控遊戲節奏。
  - **2026-05-21 邊界穿透模式**：已實作「撞牆不死，邊界穿透」機制。蛇頭撞牆時不會引發遊戲結束，而是會以模數（Modulo）對齊公式從棋盤的相反一側重新出現；僅有撞擊自己身體時才會判為 Game Over。同時亦同步修正了右側面板的「遊戲說明」清單。
  - **2026-05-21 音效機制優化與隨機自訂音效播放**：已實作全螢幕太陽慶祝特效音效隨機播放。每當遊戲中任何太陽特效觸發時，會隨機從 `public/` 目錄中挑選並播放以下兩首自訂音效之一：
    * `/China.mp3`（對應 `public/China.mp3`）
    * `/China_2.mp3`（對應 `public/China_2.mp3`）
    * *修復記錄*：
      1. 原程式碼中音效路徑寫為小寫，而 `public` 目錄下的實際檔案首字母為大寫，導致 404 錯誤而無音效，已修正一致。
      2. 原解鎖邏輯在每一次鍵盤按鍵或按鈕點擊時都會重複執行，並且會對正式音效快取呼叫 `play()` 再 `pause()`，導致剛好在播放中的太陽特效音訊在玩家操作時會立刻被中斷。已重構為：宣告 `isAudioUnlocked` 控制僅執行一次，並改用獨立的臨時無聲 WAV Base64 來完成瀏覽器 Autoplay 的解鎖，完全解耦正式音效，徹底解決玩家操作時會意外掐斷/暫停音效的 Bug！
      3. *解決沒有聲音的致命 Bug*：原 `AudioContext` 建立在 Promise 異步微任務中，會因脫離使用者手勢同步調用棧而導致維持 Suspended（被瀏覽器阻擋無聲）。已將其初始化與 `resume()` 移入 `unlockAudio()` 的 **「最前線同步調用棧」** 執行，100% 保證在任何瀏覽器中皆能成功發聲！
      4. *修復 HTML table 語法致命警告*：原本 `score-table` 中的 `<tr>` 直接作為 `<table>` 的子元素，不符合標準 HTML5 規範，導致 Vite 插件在嚴格語法檢驗下編譯崩潰（Exit code 1），網頁沒能正常重載。已在 `<tr>` 外層包覆 `<tbody>` 標籤，順利通過 Vite 編譯並正常運作！
    同時，為確保能在現代瀏覽器的 Autoplay 安全政策下 100% 成功播放，並防止重複載入造成的效能損耗，實作了「全域 Audio 元件雙快取」與「互動式無聲預載解鎖機制」。當玩家操作鍵盤或點選按鈕時，會自動嘗試解鎖與快取音訊；並在工具列貼心提供高質感的冰綠色發光「🔊 音效 ON / 🔇 音效 OFF」開關，供玩家自主切換。
  - **2026-05-21 新增吃到食物音效 (Web Audio API 合成)**：為大幅提升蛇吃食物時的打擊反饋感與悅耳感，全新實作了吃到食物時的清脆電子反饋音效。由於 `public/` 目錄不適合擺放太繁重且頻繁觸發的 mp3 檔，我使用高超的 Web Audio API 技術（由 `AudioContext` 驅動），在瀏覽器記憶體中動態生成一個 0.1 秒、紅白機復古風格（正弦波 sine 從 C5 滑音至 G5）的清脆「叮」電子音效。該方案無須額外下載資源檔案、完美避免 404 且具備零延遲高回饋的極致遊玩反饋！**（音量優化：已將初始 gain 值由原先的 0.08 調大至 0.3，顯著提升了吃食物時的打擊回饋感與清脆度。）**
  - **2026-05-21 網頁進入首頁警告遮罩**：全新實作剛進入遊戲時的全黑儀式感遮罩。畫面中央呈現神祕黑洞光暈與浮動墨鏡圖示，並顯示黃金警告字句 **「為避免陽光照射，請戴上墨鏡遊玩」**。此頁面除了完美扣合遊戲主題與極致豐富美學（Rich Aesthetics）外，更具備核心技術價值：玩家點擊 **「戴上墨鏡 😎 進入遊戲」** 按鈕時，會直接主動觸發 `unlockAudio()` 解鎖音訊，完美、優雅地解決了瀏覽器 Autoplay 自動播放限制，使遊戲內所有隨機音效在後續能 100% 播放成功！
  - **2026-05-21 遊戲狀態遮罩 (Overlay) 位置對齊與美化**：已成功將「等待開始、暫停中、遊戲結束」這三個狀態 Overlay 移入 `.game-board` (棋盤/地圖) 的 DOM 內部。由於棋盤本身具有 `position: relative` 定位，此更動讓 Overlay 能夠 ** 100% 完美、精準地居中覆蓋在地圖正中央 **，解決了原本因為包含右側說明面板而導致整體畫面偏右或偏離地圖的問題。此外，全新重構了 Overlay 樣式，引進毛玻璃濾鏡 (`backdrop-filter: blur(6px)`)、深邃科技感暗藍背景 (`rgba(10, 15, 30, 0.9)`) 以及紫色發光陰影，使整體介面質感更臻完美！
  - 300分以上：每 50 分延續觸發重疊太陽，作為後續遊玩機制。
  - **2026-05-21 特效鎖定與 300 分棉花美白機制實作**：
    1. *150 分特效文字鎖定修正*：徹底解決了因為蛇在全螢幕特效 3.5 秒期間吃食物得分（如從 150 變 160），進而導致原本的 `celebrationCongratsText` 計算屬性重算洗回預設「太陽越來越耀眼」的 Bug。已改用在 `triggerSunCelebration` 觸發當下「鎖定」分數與文字的響應式變數 `celebrationScore` 與 `celebrationText`，確保畫面的特效與動畫完美同步且不被中途干擾。
    2. *300 分以上食物變棉花 ☁️*：分數大於等於 300 分時，地圖上的太陽食物將動態轉變為白色蓬鬆、帶有發光暈圈與 `cottonPulse` 呼吸起動畫的「棉花 ☁️」，帶來極佳的反差美感。
    3. *300 分以上暫停畫面黑化與特別台詞*：分數大於等於 300 分時，暫停 Overlay 會切換至純黑毛玻璃的黑洞背景樣式 (`.blackout-overlay`)，主標題動態顯示為「你曬黑了」，副標題動態顯示為「請收集棉花賺錢，去做美白」，並增加閃爍呼吸提示「按空白鍵繼續收集」，極具驚喜與沉浸式幽默。
    4. *300 分曬黑點自動暫停機制*：已成功實作吃到 300 分（及每 300 分曬黑週期點）時自動呼叫 `pauseGame()` 暫停遊戲。如此一來，在全螢幕純黑太陽特效播完後，會完美顯示「你曬黑了，請收集棉花賺錢，去做美白」的黑洞暫停警告畫面，直到玩家按下空白鍵繼續，徹底修正先前未自動暫停的 Bug。
  - **2026-05-21 完美實作「曬黑 ➔ 採棉花 ➔ 美白 ➔ 耀眼」無限故事迴圈與白色/黑白太陽重疊特效**：
    1. *無限故事迴圈*：在 `moveSnake` 得分觸發點全面以模數計算（300 分週期：300 曬黑、350/400 黑白重疊、450 美白成功、500 普通耀眼、550 陽光強烈），使玩家在 300 分後進入棉花黑夜與美白循環，極富故事張力與遊戲可玩性。
    2. *白色太陽特效*：在分數達到 450 分（及每週期美白成功點）時，調用 `/sun_white.png` 特效，並設計了多層次極致銀白發光的 CSS `.sun-white-img` 動畫效果，展現令人驚嘆的視覺質感。
    3. *黑白太陽重疊*：在 300~440 分的棉花黑夜中（如 350、400分），全螢幕慶祝會以聖潔白太陽為底，疊加偏移動態抖動旋轉的黑色太陽殘影頂層，並以高質感大字顯示「離美白越來越近」。
    4. *慶祝多行文字支援*：優化 `.sun-congrats` 的 CSS 樣式，將 `white-space` 改為 `pre-line`，並加上 `text-align: center` 支援換行與居中，確保長句及多行說明能精美呈現。
  - **2026-05-21 整合型設定選單實作**：為了提高介面整潔度與視覺豐富美學（Rich Aesthetics），已將原本散落於工具列的「網格線顯示」、「高分加速機制」及「遊戲音效」三個獨立按鈕，完美整合進一個精美的「⚙️ 設定」按鈕中。點擊該按鈕會以淡入及微幅滑落的過渡動畫（`slide-up`）展開一高質感的毛玻璃（`backdrop-filter`）、科技感暗夜藍背景、青色微光發光邊框的下拉選單。此選單特別設計了全螢幕透明遮罩 `.settings-backdrop` 供玩家點選外部時自動收合，且保留了點按時順暢解鎖瀏覽器 Autoplay 音訊的功能，大幅提升使用者介面的科技奢華感！
  - **2026-05-21 太陽慶祝音效交替播放優化**：為了解決因 Math.random() 隨機機率重複（例如 50、100、150、200分時剛好都隨機到同一首）讓玩家產生「沒有隨機播放」的錯覺，並防止多重 HTMLAudioElement 在嚴格瀏覽器中部分解鎖失敗的隱患，我將音訊播放架構徹底重構為「單一全域 Audio 實例動態切換 src」搭配「強制交替播放」機制。在預載解鎖時直接以該實例播放無聲 WAV 取得瀏覽器綠燈；每次觸發太陽特效時，會自動比對並強制播放與上一次不同的音訊檔（`/China.mp3` 與 `/China_2.mp3` 輪流交替），100% 確保玩家每次百分慶祝時，耳邊響起的旋律絕對截然不同！
  - **2026-05-27 實作與重構進入/繼續遊戲音效**：最初設計了 `startAudio` 獨立實例，但受限於現代瀏覽器嚴格的 Autoplay 安全政策（非同步 Microtask 會失去手勢解鎖憑證），隨後將其優雅地重構為**「全域單一 Audio 實例 (`sunAudio`) 共享與 src 動態切換」**架構。在玩家點擊「戴上墨鏡 😎 進入遊戲」的同步點擊 Callstack 中，直接對 `sunAudio` 設定 `/start.mp3` 並實施播放。此舉不僅完美、高相容性地響起了開場啟動音效，還以最高效的同步形式完成了該唯一 Audio 實例在瀏覽器中的完美解鎖，保證了後續所有慶祝音效 (`China.mp3` 系列) 的播放無阻！
  - **2026-05-27 「電馭叛客未來科技美學」全域視覺重構與實體鍵盤亮鍵互動**：
    1. *太空科技暗夜背景*：將死板且刺眼的亮白色背景，優雅重構為深邃藏青至暗紫的宇宙徑向漸層，並在深處加入兩團以 CSS 關鍵影格緩緩旋轉漂移的霓虹星雲光暈（Aurora Lights），完美解放毛玻璃卡片與發光陰影的霓虹魅力。
    2. *極致毛玻璃流光卡片*：升級說明卡片 (`.info-block`)，引進高透光率的 `backdrop-filter: blur(16px)` 與半透明科技藍，且卡片頂部邊框額外加入半透明青色發光細線，配上專屬「青色科技定位飾條」與 Icon 浮動脈衝動畫。
    3. *科幻戰機儀表板計分板*：頂部計分面板加入幾何定位點與動態垂直循環滑動的橫向極細掃描線動畫 (`scoreboardScan`)，逼真展現科技控制儀表螢幕。
    4. *色彩能量流動蛇身與發光眼睛*：蛇身加入色彩漸變在身體裡順滑無縫滾動的流體能量動畫 (`snakeEnergyFlow`)，且蛇頭眼睛注入高亮科幻霓虹發光。
    5. *3D 晶亮點燈虛擬鍵盤*：打磨按鍵的晶亮 3D 質感，並註冊 `keyup`/`keydown` 全域監聽狀態，當玩家在實體鍵盤敲擊按鍵時，畫面虛擬按鍵會同步觸發「下壓並亮起霓虹冰藍燈」的實時回饋！
    6. *極光毛玻璃狀態遮罩*：對話框升級為高透光毛玻璃 (`blur(16px)`) 與紫色高光邊界細線，大幅增強前後景深立體感。
    7. *全域零邊距滿版無縫修復*：修正了 `main.ts` 原本漏掉引進 `main.css`，以及 `#app` 的 `max-width` 和 `padding` 導致四周露出刺眼白色邊框的 Bug。將全域 `margin`/`padding` 歸零，使未來科技感的遊戲背景能夠 100% 滿版無縫地擴展至整個瀏覽器視窗，完美融為一體！
- 修正了 HTML 特效中「日食」的錯字（原本為日蜘）。
